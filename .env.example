# daily-cockpit — Environment Variables
# Copy to .env and fill in your values.

# Slack user OAuth token (xoxp-). MUST be provisioned with READ-ONLY scopes only:
#   channels:history, channels:read, groups:history, groups:read,
#   im:history, im:read, mpim:history, users:read
# Do NOT grant chat:write, channels:write, reactions:write, or any write scope.
SLACK_USER_TOKEN=xoxp-your-token-here

# Google accounts to monitor, with per-account scope control.
# Format: email:scope,email:scope,...
# Scopes: 'email' (inbox only), 'cal' (calendar only), 'both' (email + calendar)
# Google OAuth MUST use read-only scopes only:
#   gmail.readonly, calendar.readonly
# Do NOT grant gmail.send, gmail.modify, calendar.events, or any write scope.
GOG_ACCOUNTS=your-primary@gmail.com:both,your-secondary@gmail.com:email

# Private Discord channel ID for briefings, digests, alerts, and Q&A.
DISCORD_CHANNEL=your-discord-channel-id
